
# TODO List - Leaderboard Fixes - COMPLETED ✅

## Tasks Completed
- [x] 1. Add CSS for offline-badge to show if players are offline in leaderboard
- [x] 2. Fix userId key in monthly-leaderboard.js (change 'aimtrainer_user_id' to 'aimtrainer_userid')

## Summary of Changes

### Fix 1: Offline Badge CSS (index.html)
Added CSS styling for `.offline-badge` class:
```css
/* Offline Badge */
.offline-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-left: 8px;
    font-size: 12px;
    opacity: 0.7;
    cursor: help;
}
```

### Fix 2: UserId Key Consistency (monthly-leaderboard.js)
Changed all instances of `aimtrainer_user_id` to `aimtrainer_userid` to match:
- firebase-leaderboard.js which uses `aimtrainer_userid`
- firebase-auth.js which uses `aimtrainer_userid`

This ensures the monthly/weekly leaderboard can properly:
- Track user's own scores using the correct localStorage key
- Show other players' scores from localStorage
- Combine real users with bot/offline players

